<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ZF2 Hotel Demo</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 0; background: #f4f4f4; }
        nav { background: #2c7a2c; padding: 0 30px; display: flex; align-items: center; }
        nav a { color: #fff; text-decoration: none; padding: 14px 16px; display: inline-block; }
        nav a:hover { background: #1e5a1e; }
        nav .brand { font-weight: bold; font-size: 1.1em; margin-right: 20px; }
        .container { background: #fff; padding: 30px; border-radius: 5px; max-width: 900px; margin: 30px auto; }
        h1 { color: #2c7a2c; }
        a { color: #2c7a2c; }
        code { background: #f0f0f0; padding: 2px 6px; border-radius: 3px; font-size: 0.9em; }
        table { border-collapse: collapse; }
    </style>
    <script>
        let user_name = 'Ada'
        alert(`Hello ${user_name`)
    </script>
</head>
<body>
    <!-- Navigation bar — all URLs generated with $this->url() from route names -->
    <nav>
        <a href="<?php echo $this->url('home'); ?>" class="brand">Hotel Demo</a>
        <a href="<?php echo $this->url('room'); ?>">Rooms</a>
        <a href="<?php echo $this->url('room/search'); ?>">Search</a>
        <a href="<?php echo $this->url('room-about'); ?>">About</a>

        <?php if (isset($_SESSION['user_id'])): ?>
            <span style="color: #afd6af; margin-left: auto; padding: 14px 16px;">
                <?php echo $this->escapeHtml($_SESSION['username']); ?>
                (<?php echo $this->escapeHtml($_SESSION['user_role']); ?>)
            </span>
            <a href="<?php echo $this->url('auth/logout'); ?>">Logout</a>
        <?php else: ?>
            <a href="<?php echo $this->url('auth/login'); ?>" style="margin-left: auto;">Login</a>
        <?php endif; ?>
    </nav>

    <div class="container">
        <?php echo $this->content; ?>
    </div>
</body>
</html>
